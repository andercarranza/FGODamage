<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ander.fgodamagerecovery">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoadServants" />
        <activity android:name=".Confirm" />
        <activity android:name=".EditEnemy" />
        <activity android:name=".EditServant1" />
        <activity android:name=".CardSelect" />
        <activity android:name=".Calculate" />
        <activity android:name=".LoadEnemy" />
        <activity android:name=".SavedTeams" />
        <activity android:name=".EditSavedClass" />
        <activity android:name=".ModifierEdits" />
    </application>

</manifest>